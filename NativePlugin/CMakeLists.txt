CMAKE_MINIMUM_REQUIRED(VERSION 3.0.0)
PROJECT(TextureHelper)

FILE(GLOB SRC
    TextureHelper.*
) 
ADD_LIBRARY(${PROJECT_NAME} SHARED ${SRC})
TARGET_COMPILE_DEFINITIONS(${PROJECT_NAME} PUBLIC
    BUILD_TEXTUREHELPER
)
